package func;
// 類別(設計圖)
public class People {

	double height; // 屬性
	double weight;
	//static: 表示該屬性屬於類別，稱為類別成員/靜態屬性
	static double overWeightBMI = 24; // 設定一個常數，一樣是一個屬性
	
	// 物件方法:
	public double getBMI() {
		double bmi = weight/(height*height);
		return bmi;
		
	}
	
}
